<div class="row">
  <div class="col-xs-8">

<p id="notice"><%= notice %></p>

<h1>Pratos</h1>

<table class="table">
  <thead>
    <tr>
      <th>Nome</th>
      <th>Preço</th>
      <th>Tempo de preparo</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @meals.each do |meal| %>
      <tr>
        <td><%= link_to meal.name, meal %></td>
        <td><%= meal.price %></td>
        <td><%= meal.cookingTime %></td>
        <td><%= link_to 'Alterar', edit_meal_path(meal) %></td>
        <td><%= link_to 'Remover', meal, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Novo prato', new_meal_path %>

  </div>
</div>

